Milý zákazníku,

zasíláme Vám shrnutí Vaší objednávky v internetovém obchodě <%= AppConfig.short_title %>.

_______
Shrnutí

Objednávka číslo: #<%= @order.id %>

Zboží:
<% @order.products.each do |product| %>
  <%= product.title %>, <%= product.count %> kusů, <%= format_price product.cost %> (<%= format_price product.price %> za kus)
<% end %>

Celkem za zboží: <%= format_price @order.sum %>

Celková suma včetně dopravy: <%= format_price(@order.sum + @order.payment_method.cost)%>

Vaše zpráva pro nás:
 <%= @order.message %>

<% unless @order.payment_method.info.blank? %>
_____________
Způsob dodání

<%= @order.payment_method.info %>
<% end %>


Děkujeme Vám a přejeme hezký den
